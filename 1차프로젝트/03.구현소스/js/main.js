// Beanpole 메인 JS //

document.addEventListener('DOMContentLoaded', loadFn);

function loadFn(){
    console.log('로딩 완료!');

   /*  const slider=document.querySelectorAll('.slider'); */

} /////////////// loadFn 함수 ///////////////
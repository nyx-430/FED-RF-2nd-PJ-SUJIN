// Beanpole 메인 JS //

let pgNum=0;
let stsWheels=false;

const elePage=document.querySelectorAll(.)

/* document.querySelector("html").style.scrollBehavior = "smooth";
setTimeout(() => {
  window.scrollTo(0, document.querySelector("#ban-area").offsetTop);
}, 8000); */